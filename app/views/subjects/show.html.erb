
<div class="">

<% @subject.lessons.each_with_index { |lesson, i| %>
    <div class="col4 <%= ((i>1 )&&( (i+1) % 3==0 )) ? 'last' : '' %>">
      <div class="subject">
        <div class="name">
<%= link_to 'more',get_more_lessons_path %>
          <%= link_to_if lesson.classifications.count > 0, "第 #{lesson.position} 关", start_lesson_path( lesson ), data:{ lightbox: "image" } %>

          <%= render partial: 'words', locals: { lesson: lesson } %>

        </div>
        <div class="word_count"> <%= lesson.word_count %>个单词 </div>
      </div>
    </div>
<% } %>
</div>

<%= javascript_tag do %>
$.facebox.settings.closeImage = <%== image_path( 'facebox/closelabel.png').to_json %>
$.facebox.settings.loadingImage = <%== image_path( 'facebox/loading.gif').to_json %>
<% end %>
